[project]
name = "trading-agents"
version = "0.1.0"
description = "Evidence-backed stock research skill runtime helpers."
requires-python = ">=3.11"
dependencies = [
  "yfinance>=0.2.65",
]

[tool.uv]
package = false
